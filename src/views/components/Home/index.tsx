import React from "react";
import { withLayout } from "../../../layout/Layout";

const Home = () => {
  return <div>indexxxxxxx</div>;
};

// Chỉ xuất một lần với HOC áp dụng
export default withLayout(Home);
